# rick-and-morty-laravel

To run containers
docker compose -f docker-compose.yml up -d

To build
npm run dev

clear cache
php bin/console cache:clear

To access the project
http://localhost:{port}/search/characters/dimension